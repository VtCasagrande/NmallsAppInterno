server {
    listen 80;
    server_name localhost;

    # Servir arquivos estáticos do frontend
    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html;
    }

    # Configurações para cache e headers
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg)$ {
        root /usr/share/nginx/html;
        expires 1y;
        add_header Cache-Control "public, max-age=31536000";
        access_log off;
    }

    # Redirecionar requisições da API para o backend
    location /api/ {
        # Usando variável interna para determinar o host do backend
        # O padrão é localhost:5000 se não for configurado de outra forma
        set $backend_host "localhost:5000";
        proxy_pass http://$backend_host/api/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
} 